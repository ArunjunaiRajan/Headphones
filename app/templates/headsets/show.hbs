<div class="jumbotron container media text-center">
<img src="{{model.image}}" class="d-flex mt-3">
  <div class="media-body"><h1 class="display-2">{{model.title}}</h1>
    <hr>
    <div class="d-inline">
      <h3 class="lead">{{model.type}}</h3>
    </div>
    <div class="d-inline">
      <h3 class="lead">{{model.rate}}</h3>
    </div>
    <hr>
    <div class="align-self-center">
        <p >{{model.description}}</p></div>
  </div>
  </div>
{{outlet}}
